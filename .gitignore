# Environment / secrets — placeholder .env was committed once for variable
# discoverability; real values are populated locally and must never land here.
.env

# BMAD tooling overlay (installer regenerates; personal configs live inside)
_bmad/

# Personal Claude Code settings
.claude/settings.local.json

# BMAD-installed skills (boilerplate; regenerate via `pnpm install:bmad`)
.claude/skills/bmad-*
